galaxy_info:
  author: Markus Koch
  description: Quickly creates disk configuration
  license: Apache 2.0
  min_ansible_version: 2.2
  platforms:
  - name: Fedora
    versions: [ 24, 25, 26, 27 ]
  - name: EL
    versions: [ 6, 7 ]

  galaxy_tags: [ 'system', 'lvm', 'disksetup', 'beta' ]

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
